#if !defined(HASH_H)
#define HASH_H

#include <iostream>

/*
 * String.hashCode() from Java.
 *  f(s) = s[0] * 31^(n-1) + s[1] * 31^(n-2) + ... + s[n-1]
 *  - s: Input string.
 *  - n: Size of the string.
 */
int javaStringHashCode(std::string s) {
    // TODO
    
}

#endif  // HASH_H
